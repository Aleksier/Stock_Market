package com.example.stockmarket

import android.content.Context
import androidx.lifecycle.LiveData
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext
/*
class FavoriteCompaniesRepository(applicationContext: Context) {

   private val Db = AppDatabase.getInstance(applicationContext)

    suspend fun addCompany(favoritesCompanies: FavoritesCompanies) {
        Db.favoritesCompaniesDao().insert(favoritesCompanies)
    }
}

*/